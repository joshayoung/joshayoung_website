import React from "react";

export default () => (
  <dl>
    <dt>Output HTML Boilerplate</dt>
    <dd>
      <code>html:5 + ENTER</code>
    </dd>

    <dt>Open VS Code Settings</dt>
    <dd>
      <code>CMD/CTRL + ,</code>
    </dd>

    <dt>Change Values Incrementally</dt>
    <dd>
      <code>Select a variable then</code>
      <code>CMD/CTRL + D to select next instance</code>
      <code>Enter insert mode and change all of the selected values</code>
    </dd>
  </dl>
);
