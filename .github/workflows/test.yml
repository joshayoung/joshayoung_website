name: 'gatsby processes'

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
        env:
          GATSBY_TOKEN: ${{ secrets.TEST_ME }}