    <section>
      <header>
        <a href="/tags">Tags</Link>
      </header>
      <ul aria-labelledby="tags">
        {dta.map(tag_link => (
          <li key={tag_link}>
            <a href={`/tags/${tag_link}`}>{formatTag(tag_link)}</a>
          </li>
        ))}
      </ul>
    </section>